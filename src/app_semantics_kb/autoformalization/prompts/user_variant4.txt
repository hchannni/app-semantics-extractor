**[Current Task]**
Use the provided inputs below to extract and generate State Predicates for the app.
If `Existing Predicates` already contain a relevant predicate, output an updated version using the same `name` so it can be merged.

**[Input Sources]**
- App Screenshot Images: (Images attached here)
- App Name: {app_name}
- Existing Predicates: {existing_predicates}
- Accessibility Tree(Optional): {accessibility_tree}
- Sliced Methods Source Context:
  Structure:
    1. [METHOD N] blocks — source code (method_full_name + file + code)
    2. Domain Type Context — full data model definitions
    3. Resource→Method Index
    4. Call Path Overview
    5. CFG Details — basic block listings per method
---------------------------------------------------
{sliced_methods_source_blob}
---------------------------------------------------


Begin!

Created Predicates:
