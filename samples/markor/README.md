# Markor sample

The source application is fetched from the upstream repository at the commit recorded in
`app_manifest.json`; it is not vendored here. Run `fetch_app.sh` to clone the pinned revision.
